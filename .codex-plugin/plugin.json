{
  "name": "agent-mcp-tester",
  "version": "0.0.8",
  "description": "E2E-tests MCP servers: LLM-driven sweeps that record repeatable test suites, plus a deterministic runner that replays them with zero LLM tokens.",
  "author": {
    "name": "Arne von Appen"
  },
  "repository": "https://github.com/Seretos/agent-mcp-tester",
  "mcpServers": {
    "mcp-tester": {
      "command": "${PLUGIN_ROOT}/bin/mcp-tester",
      "args": []
    }
  },
  "skills": "./skills",
  "interface": {
    "displayName": "Agent MCP Tester",
    "shortDescription": "E2E-tests MCP servers: LLM-driven sweeps that record repeatable test suites, plus a deterministic runner that replays them with zero LLM tokens."
  }
}
