__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
build/
dist/
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.log
.vscode/
.idea/

# Claude Code local runtime artifacts
.claude/scheduled_tasks.lock

# PyInstaller output — produced locally by scripts/build.ps1 and
# shipped to end users via GitHub Releases, not the repo. Whole
# bin/ tree so the Linux-side `bin/mcp-tester` (no extension) is
# also covered, not just the Windows .exe.
/bin/
*.pyinstaller.tmp/
